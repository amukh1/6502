MEMORY {
    ZP:     start = $0002, size = $00FE, type = rw, define = yes;
    RAM:    start = $0200, size = $6000, type = rw, define = yes;
    ROM:    start = $8000, size = $8000, type = ro, define = yes;
}

SEGMENTS {
    ZEROPAGE: load = ZP,  type = zp;
    DATA:     load = RAM, type = rw;
    CODE:     load = ROM, type = ro;
}